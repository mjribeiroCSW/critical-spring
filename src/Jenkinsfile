pipeline {
    agent any

    stages {
        // checkout de master, based on jenkins configuration
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        // builds the java project
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }

        // runs the unit tests
        stage('Unit Test') {
            steps {
                sh 'mvn test'
            }

            post {
                always {
                    junit '**/target/surefire-reports/TEST-*.xml'
                }
            }
        }

        // Run integration tests
        stage('Integration Test') {
            steps {
                sh 'mvn verify -Pintegration-test'
            }

            post {
                always {
                    junit '**/target/failsafe-reports/TEST-*.xml'
                }
            }
        }

        // Add more stages as needed (e.g., deployment, additional tests, etc.)
    }

    // Add post actions, notifications, or other global settings if needed
}